% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ModelError.R, R/pfim-rd-examples.R
\name{ModelError}
\alias{ModelError}
\title{ModelError}
\usage{
ModelError(
  output = "output",
  sigmaInter = 0,
  sigmaSlope = 0,
  sigmaInterFixed = FALSE,
  sigmaSlopeFixed = FALSE,
  cError = 1,
  varianceForm = "combined1",
  ...
)
}
\arguments{
\item{output}{Outcome name.}

\item{sigmaInter}{Additive residual SD.}

\item{sigmaSlope}{Proportional residual SD.}

\item{sigmaInterFixed}{If \code{TRUE}, \code{sigmaInter} is not estimated.}

\item{sigmaSlopeFixed}{If \code{TRUE}, \code{sigmaSlope} is not estimated.}

\item{cError}{Power on the proportional prediction term.}

\item{varianceForm}{\code{"combined1"} or \code{"combined2"} (subclasses set this).}

\item{...}{Legacy \code{equation}/\code{derivatives} warn and are ignored.}
}
\value{
An S7 object of class \code{ModelError}.
}
\description{
Base class for residual error models (\code{Constant}, \code{Combined1},
\code{Combined2}, \code{Proportional}).

Variance is determined by \code{sigmaInter} (additive SD),
\code{sigmaSlope} (proportional SD), \code{cError}, and
\code{varianceForm}:
\itemize{
  \item \code{"combined1"}: \eqn{V=(a + b f^{c})^2}
  \item \code{"combined2"}: \eqn{V=a^2 + (b f^{c})^2} (PopED-style)
}
A sigma is estimable in the FIM iff it is non-zero and not marked fixed
(\code{sigmaInterFixed} / \code{sigmaSlopeFixed}).
}
\examples{
ModelError()
}
